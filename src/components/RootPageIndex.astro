---
import Layout from "@layouts/Layout.astro";
import Container from "@components/Container.astro";

type Props = {
  title: string;
  pagefindIgnore?: boolean;
};

const { title, pagefindIgnore } = Astro.props;
---

<Layout title={title}>
  <Container>
    <aside {...pagefindIgnore ? { "data-pagefind-ignore": true } : {}}>
      <div class="space-y-10">
        <h1 class="animate font-semibold text-black dark:text-white">
          {title}
        </h1>
        <div class="space-y-4">
          <slot />
        </div>
      </div>
    </aside>
  </Container>
</Layout>
